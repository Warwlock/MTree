{
  "git": {
    "commitMessage": "Release v${version}",
    "tagName": "v${version}"
  },
  "github": {
    "release": true,
    "releaseName": "v${version}"
    "tokenRef": "GITHUB_TOKEN"
  },
  "npm": {
    "publish": true
  }
}